<div class="l-tiles js-tiles">
    <div class="tiles__inner l-contain">
        {{#each items}}
        {{#if inner}}
        <div class="tile tile--inner js-tile" tabindex="0">
        {{else}}
        <div class="tile">
        {{/if}}
            <div class="tile__content">
                <div class="tile__icon-container">
                    <img class="tile__icon" src="/images/{{icon}}" alt="">
                </div>
                <h3 class="tile__title">{{title}}</h3>
                {{#if inner}}<div class="tile__inner">{{{inner}}}</div>{{/if}}
            </div>
        </div>
        {{/each}}
    </div>
</div>
